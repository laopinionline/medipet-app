# Credenciales Admin SDK — NUNCA commitear (proyecto medipet-c3a4d)
seed/serviceAccountKey.json
serviceAccountKey.json
*serviceAccount*.json

# Entorno demo (medipaw-demo) — key + passwords fijos: NUNCA commitear
seed/serviceAccountKey.demo.json
seed/demo-secrets.json

# Dependencias
node_modules/
seed/node_modules/

# Logs / temporales
*.log
.DS_Store
